#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
        int mx=0;

    int n;
    cin>>n;
    while(n--){
        int b;
        cin>>b;
        mx=max(mx,b);

    }
    cout<<mx<<endl;


    return 0;
}