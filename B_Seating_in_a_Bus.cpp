#include <bits/stdc++.h>
using namespace std;
typedef long long ll;
int main()
{
    cin.tie(0);cin.sync_with_stdio(0);
    cout.tie(0);cout.sync_with_stdio(0);
    int t = 1;
    cin >> t;
    while (t--)
    {

windows was also good as .. akhir mere ek hi windows notebook se sara kaam ho jaa rha tha.. aab ssb aalg alg krna pd trha hai ..
but prof is using in the dual boot so i should also use it  }
    return 0;
}